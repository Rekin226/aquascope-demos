# Data

Describe each input file in one line.

| File | Source | Last fetched | Notes |
|------|--------|--------------|-------|
| `<file_name>` | `<URL or DOI>` | YYYY-MM-DD | License / size / format notes |

If any single file exceeds 5 MB, replace it with `fetch.py` (or `fetch.sh`) and document the command in the table above.
